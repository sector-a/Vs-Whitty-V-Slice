import funkin.ui.story.Level;
import flixel.FlxG;

class UnknownLevel extends Level
{
	function new()
	{
		super('bpunknown');
	}

	override public function isUnlocked(){
		super.isUnlocked();

		return FlxG.save.data.isUpdikeUnlocked;
	}

	override public function isVisible(){
		super.isVisible();

		return FlxG.save.data.isUpdikeUnlocked;
	}

	override function getSongDisplayNames(difficultyId:String):Array<String> {
		return [""];
	}
}